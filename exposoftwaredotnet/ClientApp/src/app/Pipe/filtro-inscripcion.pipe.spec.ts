import { FiltroInscripcionPipe } from './filtro-inscripcion.pipe';

describe('FiltroInscripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroInscripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
