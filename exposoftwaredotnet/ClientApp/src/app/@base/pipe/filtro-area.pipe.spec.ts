import { FiltroAreaPipe } from './filtro-area.pipe';

describe('FiltroAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
