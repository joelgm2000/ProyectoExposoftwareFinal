import { DescripcionCalificacion } from './descripcion-calificacion';

describe('DescripcionCalificacion', () => {
  it('should create an instance', () => {
    expect(new DescripcionCalificacion()).toBeTruthy();
  });
});
